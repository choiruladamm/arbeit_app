import 'package:flutter/cupertino.dart';

class AllJobsTabBar extends StatelessWidget {
  const AllJobsTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('All Jobs'),
    );
  }
}